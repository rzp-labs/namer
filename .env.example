# Copy this file to .env and fill in the values.
# Do NOT commit your real .env (it is ignored by .gitignore).

# ThePornDB API token (used for metadata lookups)
TPDB_TOKEN=

# StashDB API token (optional; only used if metadata_provider=stashdb)
STASHDB_TOKEN=

# Runtime settings for the container
TZ=America/Phoenix
PUID=99
PGID=100
UMASK=000

# Namer configuration path inside the container
# For the provided compose file, this should be /app/config/namer.cfg
NAMER_CONFIG=/app/config/namer.cfg

# Intel VAAPI driver selection (required for QSV on Intel platforms)
LIBVA_DRIVER_NAME=iHD
