# Copy this file to .env and fill in the values.
# Do NOT commit your real .env (it is ignored by .gitignore).

# ThePornDB API token (used for metadata lookups)
TPDB_TOKEN=

# StashDB API token (optional; only used if metadata_provider=stashdb)
STASHDB_TOKEN=

# Runtime settings for the container
TZ=America/Phoenix
PUID=99
PGID=100
UMASK=000

# CodeRabbit CLI behavior for local reviews
# Set to 1 to enable pre-commit CodeRabbit checks (default off to conserve quota)
CODERABBIT_PRECOMMIT=0
# Set to 0 to skip CodeRabbit during validate.sh (default on for higher confidence)
CODERABBIT_VALIDATE=1
# Override the base branch used for diff comparisons (usually 'main')
CODERABBIT_BASE=main
# Extra arguments to pass to `coderabbit review` (space-delimited)
CODERABBIT_EXTRA_ARGS=

# Namer configuration path inside the container
# For the provided compose file, this should be /app/config/namer.cfg
NAMER_CONFIG=/app/config/namer.cfg

# Intel VAAPI driver selection (required for QSV on Intel platforms)
LIBVA_DRIVER_NAME=iHD
