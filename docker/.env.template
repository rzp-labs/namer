# Environment Variables Template for Namer Docker Deployment
# Copy this to .env and fill in your actual values if you want to override defaults
# Note: API keys should be set in the namer.cfg files directly

# Optional: ThePornDB configuration (if switching providers in config)
# TPDB_API_KEY=your_theporndb_api_key_here
# TPDB_ENDPOINT=https://api.metadataapi.net

# Container User/Group (should match unRAID settings)
PUID=99
PGID=100
UMASK=000

# Timezone
TZ=America/Phoenix

# Hardware acceleration (Intel QuickSync)
LIBVA_DRIVER_NAME=iHD

# Optional: Custom naming template override
# NAMER_TEMPLATE="[%D] %T - %P%S%E"

# Optional: Override log levels
# NAMER_LOG_LEVEL=INFO
# NAMER_CACHE_LOG_LEVEL=INFO
# NAMER_BACKLOG_LOG_LEVEL=INFO
